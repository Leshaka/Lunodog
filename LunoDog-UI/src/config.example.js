export const cfg = {
  // URL to authentificate a discord user to the bot, generate at dev applications page
  oauthURL: '',
  // URL to the lunodog api server
  apiURL: 'https://127.0.0.1:4300/api',
  // URL to install the bot to a discord server, generate at dev applications page
  inviteURL: ''
}